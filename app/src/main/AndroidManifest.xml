<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.computer.brewing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calculators" />
        <activity android:name=".ABV_Calculator" />
        <activity android:name=".sugarContent_Calculator" />
        <activity android:name=".Sugar_Addition_Calculator" />
        <activity android:name=".CurrentBrews" />
        <activity android:name=".BrewsCreate" />
        <activity android:name=".test" />
        <activity android:name=".BrewView" />
        <activity android:name=".BrewEdit"></activity>
    </application>

</manifest>